# A list of repos and hook IDs that are capable of detecting secrets. If a repo has multiple
# secret-detecting hooks, list all the ones that would do it. Init-time secret detection will
# occur using the first repo and hook that are in the language configuration that are also
# listed here.
"https://github.com/yelp/detect-secrets":
  - "detect-secrets"
